interface PackageManagerProps {
    readonly lockFile: string;
    readonly installCommand: string[];
    readonly runCommand: string[];
    readonly argsSeparator?: string;
}
/**
 * A node package manager
 */
export declare class PackageManager {
    static NPM: PackageManager;
    static YARN: PackageManager;
    static PNPM: PackageManager;
    static fromLockFile(lockFilePath: string): PackageManager;
    readonly lockFile: string;
    readonly installCommand: string[];
    readonly runCommand: string[];
    readonly argsSeparator?: string;
    constructor(props: PackageManagerProps);
    runBinCommand(bin: string): string;
}
export {};
